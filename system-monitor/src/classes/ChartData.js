export default class ChartData {
    constructor(title, data, yAxis, xAxis, route) {
        this.title = title;
        this.data = data;
        this.yAxis = yAxis;
        this.xAxis = xAxis;
        this.route = route;
    }
}